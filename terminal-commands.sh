# 1. Pull perubahan dari remote repository
git pull origin main

# Jika terjadi konflik, selesaikan konflik tersebut
# Kemudian commit hasil penyelesaian konflik
# git add .
# git commit -m "Resolve merge conflicts"

# 2. Setelah berhasil pull dan menyelesaikan konflik (jika ada), push kembali
git push origin main